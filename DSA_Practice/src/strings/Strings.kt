package strings

fun main() {
    val name = "UmeshTaneja"
    println(name[3])
    println(name.length)
    println(name.first())
    println(name.last())
    println(name.substring(4,11))
    println(name.contains("esh"))
}